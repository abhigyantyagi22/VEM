# Frontend environment example for VEM (Vite)
# Copy to `frontend/.env` or use during dev with your tooling.

# API backend base URL used by the frontend
VITE_API_URL=https://vem-backend-y13y.onrender.com/api
